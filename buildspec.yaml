version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo "Installing dependencies..."
      - npm install
  pre_build:
    commands:
      - echo "Running lint and tests..."
  build:
    commands:
      - echo "Building the app..."
      
artifacts:
  files:
    - '**/*'
  base-directory: dist
